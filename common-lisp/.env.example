# Docker Compose設定

# SBCLバージョン（Alpine版）
SBCL_VERSION=2.5.10-alpine3.22

# プロジェクト名（コンテナ名のプレフィックスになります）
COMPOSE_PROJECT_NAME=common-lisp

# コンテナ名
CONTAINER_NAME=common-lisp-dev

# リソース制限
MEMORY_LIMIT=512m
CPU_LIMIT=1.0

# ユーザーID（ホストと合わせる場合は変更）
USER_ID=1000

# ロケール設定
LANG=C.UTF-8
LC_ALL=C.UTF-8

# Jupyter Notebook設定
JUPYTER_CONTAINER_NAME=common-lisp-jupyter
# セキュリティ: localhostのみに公開（外部ネットワークからアクセス不可）
# 127.0.0.1 = localhost のみ, 0.0.0.0 = 全インターフェース（非推奨）
JUPYTER_HOST=127.0.0.1
JUPYTER_PORT=8888
JUPYTER_MEMORY_LIMIT=1g
JUPYTER_CPU_LIMIT=2.0

# Jupyter認証設定
# ⚠️ 重要: 本番環境では必ずランダムなトークンを設定してください
#
# トークン生成コマンド（必須）:
#   openssl rand -hex 32
#
# セキュリティ警告:
# - このデフォルト値は使用しないでください
# - トークンなし（空）は重大なセキュリティリスクです
# - 短い・予測可能なトークンは危険です
JUPYTER_TOKEN=PLEASE-CHANGE-ME-TO-RANDOM-TOKEN-RUN-openssl-rand-hex-32
